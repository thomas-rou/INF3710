export interface Bird {
    nomscientifique: string;
    nomcommun: string;
    statutspeces: string;
    nomscientifiquecomsommer: string;
}

export enum Gender {
    M = 'M',
    F = 'F',
    O = 'O'
}