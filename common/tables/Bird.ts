export interface Bird {
    scientificName: string;
    commonName: string;
    specieStatus: string;
    consumeScientificName: string;
}

export enum Gender {
    M = 'M',
    F = 'F',
    O = 'O'
}